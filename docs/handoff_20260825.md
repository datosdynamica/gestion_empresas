# Handoff operativo - estado al 2026-08-25

Fecha: 2026-08-25

## Objetivo de este handoff

Dejar una referencia corta y directa para que otra persona pueda retomar el
trabajo sin revisar todo el historial manualmente.

## Cambios cerrados en este corte

### 1. Primera factura del onboarding con descuento

Estado:

- documentado
- probado en desarrollo
- desplegado en produccion
- respaldado en Git

Archivo funcional tocado:

- `helpers/OnboardingInvoiceService.php`

Bitacora detallada:

- `docs/bitacora_onboarding_factura_descuento_20260825.md`

Commit asociado:

- `5d68db7` - `Corrige descuento en primera factura de onboarding`

Despliegue realizado:

- desarrollo: validado previamente con pruebas reales
- produccion: publicado solo el helper

Respaldo remoto de produccion:

- `/var/www/plugin/gestion_empresas/helpers/OnboardingInvoiceService.php.bak_codex_prod_20260825_120955`

Evidencia funcional resumida:

- caso de desarrollo con `RUT` valido: factura emitida y `CFE Firmado`
- el descuento quedo persistido en el item de la venta
- los rechazos previos quedaron explicados por casos de prueba con `RUT` no
  valido, no por el descuento

### 2. Panel de clientes

Estado:

- existe documentacion de inspeccion y correcciones previas
- no forma parte del despliegue puntual de descuento del 2026-08-25

Documento clave:

- `docs/clientes_panel_dev_inspeccion_20260821.md`

Commit previo relacionado:

- `feb1ed1` - `Corrige sincronizacion y documenta inspeccion del panel de clientes`

## Donde mirar primero si aparece un problema de onboarding

### Factura

- `helpers/OnboardingInvoiceService.php`
- tabla `Ventas`
- tabla `VentasItems`
- trazas del caso en `onboarding_trace.log`

### Flujo general

- `controllers/NuevasEmpresasController.php`
- `tools/process_onboarding_hitos_auto.php`
- `models/EmpresaNuevaHitoAutoModel.php`

## Regla importante de este corte

Este cierre no intento normalizar ni publicar todos los cambios sueltos del
working tree del repositorio.

Se dejo intencionalmente aislado lo siguiente:

- el commit de descuento en onboarding
- su documentacion asociada

Motivo:

- el repo ya tenia otros archivos modificados y nuevos, ajenos a este cierre
- no se debian mezclar para no perder trazabilidad ni arrastrar cambios que no
  correspondian al despliegue puntual

## Recomendacion para la siguiente persona

Si va a continuar con otro frente del proyecto:

- no asumir que todo el working tree pendiente pertenece a este ajuste
- revisar primero los commits recientes
- usar este orden de lectura:
  - `docs/handoff_20260825.md`
  - `docs/bitacora_onboarding_factura_descuento_20260825.md`
  - `docs/clientes_panel_dev_inspeccion_20260821.md`
  - `README.md`
